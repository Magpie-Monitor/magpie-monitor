apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: magpie-agent
  namespace: {{ .Values.agent.fs.namespace }}
  labels:
    name: magpie-agent
spec:
  selector:
    matchLabels:
      name: magpie-agent
  template:
    metadata:
      labels:
        name: magpie-agent
    spec:
#      tolerations:
#        # these tolerations are to have the daemonset runnable on control plane nodes
#        # remove them if your control plane nodes should not run pods
#        - key: node-role.kubernetes.io/control-plane
#          operator: Exists
#          effect: NoSchedule
#        - key: node-role.kubernetes.io/master
#          operator: Exists
#          effect: NoSchedule
      containers:
        - name: magpie-node-fs
          image: alpine
          command: ["sleep", "9999999999999999"]
#          resources:
#            limits:
#              memory: 200Mi
#            requests:
#              cpu: 100m
#              memory: 200Mi
          volumeMounts:
            - name: varlog
              mountPath: /var/log
              readOnly: true
      # it may be desirable to set a high priority class to ensure that a DaemonSet Pod
      # preempts running Pods
#      priorityClassName: important
      terminationGracePeriodSeconds: 30
      volumes:
        - name: varlog
          hostPath:
            path: /var/log